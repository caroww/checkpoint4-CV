{% extends 'base.html.twig' %}

{% block title %}HardSkills{% endblock %}

{% block body %}
    <h1>HardSkills</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ hard_skill.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ hard_skill.name }}</td>
            </tr>
            <tr>
                <th>Description</th>
                <td>{{ hard_skill.description }}</td>
            </tr>
            <tr>
                <th>Picture name</th>
                <td>{{ hard_skill.hardSkillsLogo }}</td>
            </tr>
            <tr>
                <th>Picture</th>
                <td><img style="width:50px"src="/uploads/{{ hard_skill.hardSkillsLogo }}" alt=""></td>
            </tr>           
         
        </tbody>
    </table>

    <a href="{{ path('hard_skills_index') }}">back to list</a>

    <a href="{{ path('hard_skills_edit', {'id': hard_skill.id}) }}">edit</a>

    {{ include('hard_skills/_delete_form.html.twig') }}
{% endblock %}
